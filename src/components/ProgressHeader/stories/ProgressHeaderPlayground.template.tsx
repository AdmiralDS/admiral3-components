import { ProgressHeader, type ProgressHeaderProps } from '@admiral-ds/admiral3-components';

export const ProgressHeaderPlaygroundTemplate = (args: ProgressHeaderProps) => {
  return <ProgressHeader {...args} />;
};
